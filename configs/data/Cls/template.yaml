name: "dataset_name" # configure your dataset name
type: "cls_imagenet" # keep the type as cls_imagenet
num_classes: 6

path:
  root: "./datasets/Cls/dataset_name" # data path
  split:
    train: "train"
    val: "val"
    test: "test"
    vis:
      samples: 16
      interval: 10

transform:
